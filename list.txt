Pointwise(done)
	add+relu
    bias+gelu
    silu+mul
    bias + silu + mul
Reductions(done)
    sum
    max
    mean
    argmax
Normalization(done)
    LayerNorm
    RMSNorm
    residual + layernorm
Softmax(done)
    softmax
    log_softmax
Matrix(done)
    Tiled Autotuned GEMM
    Persistent Grouped GEMM
    Persistant Grouped Split-k GEMM
Attention
    FlashAttention v1
    FlashAttention (causal)
    FlashAttention v2
Parallel Algorithms (CUDA)
    Reduction
    Prefix Sum
    Stream Compaction
    Segmented Scan 
    Radix Sort
Sampling(cuda)
    temp	
    top-k
    top-p
    multinomial
    fused sampeling
Memory & Layout(cuda)
    copy
    type_cast
    contiguous
    transpose
    permute
    slice
    gather
    scatter
Type conversions
    FP32 → FP16
    FP32 → BF16
    FP16 ↔ BF16
Packing
    INT4 pack
    INT4 unpack
Scaling
    Per-tensor
    Per-channel
    Per-token
Quantize
    FP16 → INT8
    FP16 → INT4
Dequantize
    INT8 → FP16
    INT4 → FP16
Transformer blocks
    Fused Transformer Ops
    bias + dropout + residual
    RoPe
    KV cache
    Paged kv cache


    warp specialisation
    tma 
