Pointwise(done)
	add+relu
    bias+gelu
    silu+mul
    bias + silu + mul
Reductions(done)
    sum
    max
    mean
    argmax
Normalization(done)
    LayerNorm
    RMSNorm
    residual + layernorm
Softmax(done)
    softmax
    log_softmax
Matrix
    Tiled GEMM
    Block Pointer GEMM
    Autotuned GEMM
    Batched GEMM
    Persistent GEMM
    Split-K GEMM
Attention
    QKᵀ GEMM
    Online Softmax
    Softmax @ V
    FlashAttention (non-causal)
    FlashAttention (causal)
Sampling(cuda)
    temp	
    top-k
    top-p
    multinomial
    fused sampeling
Memory & Layout
    type_cast
    copy
    contiguous
    transpose
    permute
    reshape (view)
    slice
    gather
    scatter
Type conversions
    FP32 → FP16
    FP32 → BF16
    FP16 ↔ BF16
Packing
    INT4 pack
    INT4 unpack
Scaling
    Per-tensor
    Per-channel
    Per-token
Quantize
    FP16 → INT8
    FP16 → INT4
Dequantize
    INT8 → FP16
    INT4 → FP16
Transformer blocks
    Fused Transformer Ops
    bias + dropout + residual
    RoPe
    KV cache
    Paged kv cache