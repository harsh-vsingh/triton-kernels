Pointwise(done)
	add+relu
    bias+gelu
    silu+mul
    bias + silu + mul
Reductions(done)
    sum
    max
    mean
    argmax
Normalization
    LayerNorm
    RMSNorm
    residual + layernorm
Softmax
    softmax
    log_softmax
Matrix
    GEMM(native->tiled->autotuned tiled)
Attention
    QK^T GEMM
    online softmax
    softmax @ V
    FlashAttention(Inference only, casual)
Sampling(cuda)
    temp	
    top-k
    top-p
    multinomial
    fused sampeling
Memory & Layout
    type_cast
    contiguous_copy
    transpose
    permute copy
    contiguous copy
Type conversions
    FP32 → FP16
    FP32 → BF16
    FP16 ↔ BF16
Packing
    INT4 pack
    INT4 unpack
Scaling
    Per-tensor
    Per-channel
    Per-token
Quantize
    FP16 → INT8
    FP16 → INT4
Dequantize
    INT8 → FP16
    INT4 → FP16
Transformer blocks
    Fused Transformer Ops
    bias + dropout + residual
    RoPe
    KV cache