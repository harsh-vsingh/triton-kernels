Pointwise(done)
	add+relu
    bias+gelu
    swiglu
    bias + silu + mul
Reductions(done)
    sum
    max
    mean
    argmax
Normalization(done)
    LayerNorm
    residual + RMSNorm
    residual + layernorm
Softmax(done)
    softmax
    log_softmax
Matrix(done)
    Tiled Autotuned GEMM
    Persistent Grouped GEMM
    Persistant Grouped Split-k GEMM
    Int8 quantised gemm(left)
    AWQ(left)(cuda)
    GPTQ(left)(cuda)
Attention(done)
    FlashAttention v1
        causal + non-causal
        multi-head
        cross-attention
        GQA/MQA
        ragged
    FlashAttention v2
        causal + non-causal
        multi-head
        cross-attention
        GQA/MQA
        ragged
        paged
    Decode Attention
        paged
        ragged
        dense
    Speducative Decode
        dense
        ragged
        paged
QKV projection
    Tiled Autotuned GEMM
    Persistent Grouped GEMM
    Persistant Grouped Split-k GEMM
    Int8 quantised gemm
    AWQ(cuda)
    GPTQ(cuda)
Parallel Algorithms (CUDA)
    Reduction
    Prefix Sum
    Stream Compaction
    Segmented Scan 
    Radix Sort
Sampling(cuda)
    temp	
    top-k
    top-p
    multinomial
    fused sampeling
Memory & Layout(cuda)
    copy
    contiguous
    transpose
    type_cast
    permute
    gather
    scatter
Scaling(cuda)
    Per-tensor
    Per-channel
    Per-token
    Per-group
Quantize(cuda)
    FP16 → INT8
    FP16 → INT4
Dequantize(cuda)
    INT8 → FP16
    INT4 → FP16





RoPE
QKV 
INT8 GEMM
INT8 QKV